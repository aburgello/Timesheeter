node_modules
dist
.DS_Store
*.local
.env

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# internal docs kept on disk but out of version control
docs/

# Build-only bundle report (vite.config.js visualizer). Regenerated by every
# `npm run build` with fresh content-hash filenames and uids, so tracking it
# made the tree dirty after any build while recording nothing reviewable.
bundle-stats.html
